# HeatPump Hero Dashboard
# Import via: Settings → Dashboards → Add → From YAML
# Requirements: all frontend plugins listed in info.md, plus
# kamaradclimber/heishamon-homeassistant.

title: HeatPump Hero
icon: mdi:heat-pump
views:
  # ════════════════════════════════════════════════════════════════════════
  # 1) OVERVIEW — mockup layout
  #    Row A: status chips (mode · outdoor · supply/return · freq · pressure)
  #    Row B: efficiency trend (left) | COP live/today/month/SCOP (right)
  #    Row C: schema & live values (left) | advisor summary (right)
  #    Row D: 7-day energy chart (thermal vs electrical)
  #    Row E: 7-day cycling chart
  # ════════════════════════════════════════════════════════════════════════
  - title: Overview
    path: overview
    icon: mdi:view-dashboard
    cards:

      # ── Row A: Status bar ───────────────────────────────────────────────
      - type: custom:mushroom-chips-card
        chips:
          - type: template
            icon: mdi:heat-pump
            content: "{{ states('sensor.hph_source_operating_mode') | title }}"
            icon_color: >-
              {{ 'green' if is_state('binary_sensor.hph_compressor_running','on') else 'grey' }}
          - type: template
            icon: mdi:thermometer
            content: "{{ states('sensor.hph_source_outdoor_temp') }} °C"
            icon_color: blue
          - type: template
            icon: mdi:water-thermometer
            content: >-
              {{ states('sensor.hph_source_outlet_temp') }}°C
              / {{ states('sensor.hph_source_inlet_temp') }}°C
            icon_color: red
          - type: template
            icon: mdi:sine-wave
            content: "{{ states('sensor.hph_source_compressor_freq') }} Hz"
            icon_color: >-
              {{ 'green' if is_state('binary_sensor.hph_compressor_running','on') else 'grey' }}
          - type: template
            icon: mdi:gauge
            content: "{{ states('sensor.hph_source_pump_pressure') }} bar"
          - type: template
            icon: mdi:snowflake-melt
            content: "{{ 'Defrost' if is_state('binary_sensor.hph_defrost_active','on') else '' }}"
            icon_color: cyan
            conditions:
              - condition: state
                entity: binary_sensor.hph_defrost_active
                state: "on"

      # ── Row B: KPI grid (COP + costs) — 3 columns ──────────────────────
      - type: grid
        columns: 3
        square: false
        cards:
          - type: custom:mushroom-template-card
            primary: COP live
            secondary: "{{ states('sensor.hph_cop_live') | round(2, default=0) }}"
            icon: mdi:lightning-bolt-circle
            icon_color: >-
              {% set v = states('sensor.hph_cop_live') | float(0) %}
              {% if v >= 4 %}green{% elif v >= 3 %}yellow{% elif v > 0 %}orange{% else %}grey{% endif %}
            fill_container: true

          - type: custom:mushroom-template-card
            primary: COP today
            secondary: "{{ states('sensor.hph_cop_daily') | round(2, default=0) }}"
            icon: mdi:calendar-today
            fill_container: true

          - type: custom:mushroom-template-card
            primary: COP month
            secondary: "{{ states('sensor.hph_cop_monthly') | round(2, default=0) }}"
            icon: mdi:calendar-month
            fill_container: true

          - type: custom:mushroom-template-card
            primary: SCOP season
            secondary: "{{ states('sensor.hph_scop') | round(2, default=0) }}"
            icon: mdi:calendar-star
            fill_container: true

          - type: custom:mushroom-template-card
            primary: Cost today
            secondary: "{{ states('sensor.hph_cost_today') | float(0) | round(2) }} €"
            icon: mdi:currency-eur
            icon_color: >-
              {% set v = states('sensor.hph_cost_today') | float(0) %}
              {% if v > 5 %}red{% elif v > 2 %}orange{% else %}green{% endif %}
            fill_container: true

          - type: custom:mushroom-template-card
            primary: Cost month
            secondary: "{{ states('sensor.hph_cost_monthly') | float(0) | round(2) }} €"
            icon: mdi:currency-eur
            fill_container: true
            tap_action:
              action: navigate
              navigation_path: /hph/efficiency

      # ── Row C: Schema & Advisor ──────────────────────────────────────────
      - type: horizontal-stack
        cards:
          - type: custom:mushroom-template-card
            primary: >-
              {{ states('sensor.hph_schema_variant_active') | replace('_',' ') | upper }}
            secondary: >-
              Supply {{ states('sensor.hph_source_outlet_temp') }}°C
              · Return {{ states('sensor.hph_source_inlet_temp') }}°C
              · ΔT {{ states('sensor.hph_water_spread') | float(0) | round(1) }} K
              · {{ states('sensor.hph_source_flow_rate') }} L/min
            icon: mdi:graph-outline
            icon_color: blue
            multiline_secondary: true
            tap_action:
              action: navigate
              navigation_path: /hph/schema

          - type: custom:mushroom-template-card
            primary: >-
              Advisor: {{ states('sensor.hph_advisor_summary') | title }}
            secondary: >-
              {% set advisors = [
                'sensor.hph_advisor_short_cycle',
                'sensor.hph_advisor_spread',
                'sensor.hph_advisor_heat_curve',
                'sensor.hph_advisor_heating_limit',
                'sensor.hph_advisor_dhw_runtime',
              ] %}
              {% set ns = namespace(crits=0, warns=0) %}
              {% for a in advisors %}
                {% if states(a) == 'critical' %}{% set ns.crits = ns.crits + 1 %}{% endif %}
                {% if states(a) == 'warn' %}{% set ns.warns = ns.warns + 1 %}{% endif %}
              {% endfor %}
              {% if ns.crits > 0 %}{{ ns.crits }} critical — open Optimization
              {% elif ns.warns > 0 %}{{ ns.warns }} warning(s) — open Optimization
              {% else %}All advisors OK{% endif %}
            icon: mdi:lightbulb-on
            icon_color: >-
              {% set s = states('sensor.hph_advisor_summary') %}
              {% if s == 'critical' %}red{% elif s == 'warn' %}yellow{% else %}green{% endif %}
            multiline_secondary: true
            tap_action:
              action: navigate
              navigation_path: /hph/optimization

      # ── Row D: 7-day energy chart ────────────────────────────────────────
      - type: custom:apexcharts-card
        header:
          show: true
          title: Energy — last 7 days (kWh)
        graph_span: 7d
        span:
          end: day
        series:
          - entity: sensor.hph_thermal_daily
            name: Thermal kWh
            type: column
            color: "#ff7043"
            statistics:
              type: change
              period: day
              align: start
          - entity: sensor.hph_electrical_daily
            name: Electrical kWh
            type: column
            color: "#42a5f5"
            statistics:
              type: change
              period: day
              align: start
        apex_config:
          chart:
            height: 260
          yaxis:
            title:
              text: "kWh"
          noData:
            text: "Sammele Daten — Werte erscheinen nach dem ersten vollen Tag."

      # ── Row E: COP per month (13 months) — ported from dashboard-warmepumpe ─
      - type: custom:apexcharts-card
        header:
          show: true
          title: COP — last 13 months
        graph_span: 13month
        span:
          end: month
        series:
          - entity: sensor.hph_cop_monthly
            name: COP month
            type: column
            color: "#66bb6a"
            statistics:
              type: mean
              period: month
              align: start
        apex_config:
          chart:
            height: 260
          yaxis:
            min: 0
            max: 7
            decimalsInFloat: 2
            title:
              text: "COP"
          noData:
            text: "Sammele Daten — monatliche COP-Werte erscheinen nach dem ersten vollen Monat."

      # ── Row F: Cycling last 7 days ───────────────────────────────────────
      # number.* entities don't produce HA long-term statistics (no
      # state_class on the number domain). Use group_by/func: max over
      # raw history instead — counters reset at 00:00, so the daily
      # peak == the day's total.
      - type: custom:apexcharts-card
        header:
          show: true
          title: Cycling — last 7 days
        graph_span: 7d
        span:
          end: day
        series:
          - entity: number.hph_cycles_today
            name: Cycles/day
            type: column
            color: "#66bb6a"
            group_by:
              func: max
              duration: 1d
              start_with_last: false
          - entity: number.hph_short_cycles_today
            name: Short cycles/day
            type: column
            color: "#d62728"
            group_by:
              func: max
              duration: 1d
              start_with_last: false
        apex_config:
          chart:
            height: 260
          noData:
            text: "Sammele Daten — Tageswerte erscheinen, sobald der Kompressor läuft."

  # ════════════════════════════════════════════════════════════════════════
  # 2) SCHEMATIC — auto-detected variant + Bubble-Card SVG with hotspots
  #
  # The active variant is auto-detected from which optional components
  # report data (HK2 / DHW / Buffer) and is exposed as
  # sensor.hph_schema_variant_active. Manual override via
  # select.hph_schema_variant (option "auto" follows detection).
  #
  # Each variant has its own conditional Bubble-Card with the correct
  # SVG background and variant-specific hotspot positions.
  #
  # Requires: Clooos/Bubble-Card >= 3.0
  # SVGs served from /local/hph/schema_*.svg
  # ════════════════════════════════════════════════════════════════════════
  - title: Schematic
    path: schema
    icon: mdi:graph-outline
    cards:
      # ── Setup-status / detection card ──
      - type: custom:mushroom-template-card
        primary: |-
          Installation: {{ states('sensor.hph_schema_variant_active') | replace('_',' ') | upper }}
        secondary: |-
          {% set src = state_attr('sensor.hph_schema_variant_active','source') %}
          {% set msg = state_attr('sensor.hph_schema_variant_detected','message') %}
          [{{ src }}] {{ msg }}
        icon: |-
          {% set v = states('sensor.hph_schema_variant_active') %}
          {% if 'buffer' in v %}mdi:propane-tank
          {% elif 'hk2' in v %}mdi:home-thermometer
          {% elif 'dhw' in v %}mdi:water-boiler
          {% else %}mdi:radiator{% endif %}
        icon_color: blue
        multiline_secondary: true
        fill_container: true
        tap_action:
          action: navigate
          navigation_path: /hph/config

      - type: entities
        title: Detection details
        entities:
          - entity: binary_sensor.hph_has_hk2
            name: Zone 2 detected
          - entity: binary_sensor.hph_has_dhw
            name: DHW tank detected
          - entity: binary_sensor.hph_has_buffer
            name: Buffer tank detected
          - type: divider
          - entity: select.hph_schema_variant
            name: Variant selector (auto / manual override)

      # ═══════════════════════════════════════════════════════════════════
      # Variant a2w_hk1
      # ═══════════════════════════════════════════════════════════════════
      - type: conditional
        conditions:
          - entity: sensor.hph_schema_variant_active
            state: a2w_hk1
        card:
          type: custom:bubble-card
          card_type: custom
          card_mod:
            style: |
              ha-card {
                background-image: url('/local/hph/schema_a2w_hk1.svg');
                background-size: contain;
                background-repeat: no-repeat;
                background-position: center;
                background-color: #0f1923;
                min-height: 400px;
                position: relative;
              }
          sub_buttons:
            - entity: sensor.hph_source_operating_mode
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 44%; top: 5%; background: transparent; color: #8ca8c0; font-size: 11px; font-weight: 600; }
            - entity: sensor.hph_source_outdoor_temp
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 8%; top: 14%; background: transparent; color: #e0a800; font-size: 14px; font-weight: 700; }
            - entity: sensor.hph_source_outlet_temp
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 41%; top: 22%; background: #1a252f; border: 1px solid #e74c3c; color: #e74c3c; font-size: 11px; font-weight: 700; border-radius: 6px; padding: 2px 5px; }
            - entity: sensor.hph_source_inlet_temp
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 41%; top: 70%; background: #1a252f; border: 1px solid #2980b9; color: #2980b9; font-size: 11px; font-weight: 700; border-radius: 6px; padding: 2px 5px; }
            - entity: sensor.hph_source_flow_rate
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 24%; top: 65%; background: #1a252f; border: 1px solid #2980b9; color: #4a9aca; font-size: 10px; border-radius: 6px; padding: 2px 4px; }
            - entity: sensor.hph_source_compressor_freq
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 84%; top: 71%; background: #0f1923; border: 1px solid #3d5166; color: #bcbd22; font-size: 11px; font-weight: 700; border-radius: 6px; padding: 2px 5px; }
            - entity: sensor.hph_cop_live
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 37%; top: 88%; background: #0f1923; border: 1px solid #3d5166; color: #17a2b8; font-size: 12px; font-weight: 700; border-radius: 4px; padding: 2px 5px; }
            - entity: sensor.hph_cop_daily
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 46%; top: 88%; background: #0f1923; border: 1px solid #3d5166; color: #17a2b8; font-size: 12px; font-weight: 700; border-radius: 4px; padding: 2px 5px; }
            - entity: sensor.hph_scop
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 54%; top: 88%; background: #0f1923; border: 1px solid #3d5166; color: #17a2b8; font-size: 12px; font-weight: 700; border-radius: 4px; padding: 2px 5px; }
      # ═══════════════════════════════════════════════════════════════════
      # Variant a2w_hk1_dhw
      # ═══════════════════════════════════════════════════════════════════
      - type: conditional
        conditions:
          - entity: sensor.hph_schema_variant_active
            state: a2w_hk1_dhw
        card:
          type: custom:bubble-card
          card_type: custom
          card_mod:
            style: |
              ha-card {
                background-image: url('/local/hph/schema_a2w_hk1_dhw.svg');
                background-size: contain;
                background-repeat: no-repeat;
                background-position: center;
                background-color: #0f1923;
                min-height: 420px;
                position: relative;
              }
          sub_buttons:
            - entity: sensor.hph_source_operating_mode
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 28%; top: 8%; background: transparent; color: #8ca8c0; font-size: 11px; font-weight: 600; }
            - entity: sensor.hph_source_outdoor_temp
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 9%; top: 17%; background: transparent; color: #e0a800; font-size: 14px; font-weight: 700; }
            - entity: sensor.hph_source_outlet_temp
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 35%; top: 25%; background: #1a252f; border: 1px solid #e74c3c; color: #e74c3c; font-size: 11px; font-weight: 700; border-radius: 6px; padding: 2px 5px; }
            - entity: sensor.hph_source_inlet_temp
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 35%; top: 60%; background: #1a252f; border: 1px solid #2980b9; color: #2980b9; font-size: 11px; font-weight: 700; border-radius: 6px; padding: 2px 5px; }
            - entity: sensor.hph_source_compressor_freq
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 6%; top: 82%; background: #0f1923; border: 1px solid #3d5166; color: #bcbd22; font-size: 11px; font-weight: 700; border-radius: 6px; padding: 2px 5px; }
            - entity: sensor.hph_cop_live
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 22%; top: 91%; background: #0f1923; border: 1px solid #3d5166; color: #17a2b8; font-size: 12px; font-weight: 700; border-radius: 4px; padding: 2px 5px; }
            - entity: sensor.hph_scop
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 38%; top: 91%; background: #0f1923; border: 1px solid #3d5166; color: #17a2b8; font-size: 12px; font-weight: 700; border-radius: 4px; padding: 2px 5px; }
            - entity: sensor.hph_source_dhw_temp
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 90%; top: 47%; background: #1a252f; border: 1px solid #e74c3c; color: #e74c3c; font-size: 11px; font-weight: 700; border-radius: 6px; padding: 2px 5px; }
      # ═══════════════════════════════════════════════════════════════════
      # Variant a2w_hk1_hk2_dhw
      # ═══════════════════════════════════════════════════════════════════
      - type: conditional
        conditions:
          - entity: sensor.hph_schema_variant_active
            state: a2w_hk1_hk2_dhw
        card:
          type: custom:bubble-card
          card_type: custom
          card_mod:
            style: |
              ha-card {
                background-image: url('/local/hph/schema_a2w_hk1_hk2_dhw.svg');
                background-size: contain;
                background-repeat: no-repeat;
                background-position: center;
                background-color: #0f1923;
                min-height: 460px;
                position: relative;
              }
          sub_buttons:
            - entity: sensor.hph_source_operating_mode
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 30%; top: 7%; background: transparent; color: #8ca8c0; font-size: 11px; font-weight: 600; }
            - entity: sensor.hph_source_outdoor_temp
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 9%; top: 16%; background: transparent; color: #e0a800; font-size: 14px; font-weight: 700; }
            - entity: sensor.hph_source_outlet_temp
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 32%; top: 23%; background: #1a252f; border: 1px solid #e74c3c; color: #e74c3c; font-size: 11px; font-weight: 700; border-radius: 6px; padding: 2px 5px; }
            - entity: sensor.hph_source_inlet_temp
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 32%; top: 55%; background: #1a252f; border: 1px solid #2980b9; color: #2980b9; font-size: 11px; font-weight: 700; border-radius: 6px; padding: 2px 5px; }
            - entity: sensor.hph_source_compressor_freq
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 84%; top: 81%; background: #0f1923; border: 1px solid #3d5166; color: #bcbd22; font-size: 11px; font-weight: 700; border-radius: 6px; padding: 2px 5px; }
            - entity: sensor.hph_cop_live
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 3%; top: 74%; background: #0f1923; border: 1px solid #3d5166; color: #17a2b8; font-size: 12px; font-weight: 700; border-radius: 4px; padding: 2px 5px; }
            - entity: sensor.hph_scop
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 14%; top: 74%; background: #0f1923; border: 1px solid #3d5166; color: #17a2b8; font-size: 12px; font-weight: 700; border-radius: 4px; padding: 2px 5px; }
            - entity: sensor.hph_source_dhw_temp
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 91%; top: 44%; background: #1a252f; border: 1px solid #e74c3c; color: #e74c3c; font-size: 11px; font-weight: 700; border-radius: 6px; padding: 2px 5px; }
            - entity: sensor.hph_source_zone1_temp
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 56%; top: 84%; background: #1a252f; border: 1px solid #e74c3c; color: #e74c3c; font-size: 10px; border-radius: 4px; padding: 1px 3px; }
            - entity: sensor.hph_source_zone2_temp
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 33%; top: 84%; background: #1a252f; border: 1px solid #a78bfa; color: #a78bfa; font-size: 10px; border-radius: 4px; padding: 1px 3px; }
      # ═══════════════════════════════════════════════════════════════════
      # Variant a2w_hk1_hk2_dhw_buffer
      # ═══════════════════════════════════════════════════════════════════
      - type: conditional
        conditions:
          - entity: sensor.hph_schema_variant_active
            state: a2w_hk1_hk2_dhw_buffer
        card:
          type: custom:bubble-card
          card_type: custom
          card_mod:
            style: |
              ha-card {
                background-image: url('/local/hph/schema_a2w_hk1_hk2_dhw_buffer.svg');
                background-size: contain;
                background-repeat: no-repeat;
                background-position: center;
                background-color: #0f1923;
                min-height: 500px;
                position: relative;
              }
          sub_buttons:
            - entity: sensor.hph_source_operating_mode
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 32%; top: 7%; background: transparent; color: #8ca8c0; font-size: 11px; font-weight: 600; }
            - entity: sensor.hph_source_outdoor_temp
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 9%; top: 15%; background: transparent; color: #e0a800; font-size: 14px; font-weight: 700; }
            - entity: sensor.hph_source_outlet_temp
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 16%; top: 22%; background: #1a252f; border: 1px solid #e74c3c; color: #e74c3c; font-size: 10px; font-weight: 700; border-radius: 6px; padding: 2px 4px; }
            - entity: sensor.hph_source_inlet_temp
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 16%; top: 53%; background: #1a252f; border: 1px solid #2980b9; color: #2980b9; font-size: 10px; font-weight: 700; border-radius: 6px; padding: 2px 4px; }
            - entity: sensor.hph_source_compressor_freq
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 84%; top: 22%; background: #0f1923; border: 1px solid #3d5166; color: #bcbd22; font-size: 11px; font-weight: 700; border-radius: 4px; padding: 2px 4px; }
            - entity: sensor.hph_cop_live
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 78%; top: 17%; background: #0f1923; border: 1px solid #3d5166; color: #17a2b8; font-size: 10px; font-weight: 700; border-radius: 4px; padding: 1px 4px; }
            - entity: sensor.hph_cop_daily
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 84%; top: 17%; background: #0f1923; border: 1px solid #3d5166; color: #17a2b8; font-size: 10px; font-weight: 700; border-radius: 4px; padding: 1px 4px; }
            - entity: sensor.hph_scop
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 91%; top: 17%; background: #0f1923; border: 1px solid #3d5166; color: #17a2b8; font-size: 10px; font-weight: 700; border-radius: 4px; padding: 1px 4px; }
            - entity: sensor.hph_source_dhw_temp
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 91%; top: 42%; background: #1a252f; border: 1px solid #e74c3c; color: #e74c3c; font-size: 10px; font-weight: 700; border-radius: 6px; padding: 2px 4px; }
            - entity: sensor.hph_source_buffer_temp
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 46%; top: 35%; background: #1a252f; border: 1px solid #e8a87c; color: #e8a87c; font-size: 10px; font-weight: 700; border-radius: 6px; padding: 2px 4px; }
            - entity: sensor.hph_source_zone1_temp
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 33%; top: 89%; background: #1a252f; border: 1px solid #e74c3c; color: #e74c3c; font-size: 10px; border-radius: 4px; padding: 1px 3px; }
            - entity: sensor.hph_source_zone2_temp
              show_state: true
              show_name: false
              show_icon: false
              styles: |
                .bubble-sub-button { position: absolute; left: 67%; top: 89%; background: #1a252f; border: 1px solid #a78bfa; color: #a78bfa; font-size: 10px; border-radius: 4px; padding: 1px 3px; }

  # ════════════════════════════════════════════════════════════════════════
  # 3) ANALYSIS — ApexCharts (Service-Cloud-style)
  # ════════════════════════════════════════════════════════════════════════
  - title: Analysis
    path: analysis
    icon: mdi:chart-multiline
    cards:
      - type: custom:apexcharts-card
        header:
          show: true
          title: Temperatures & compressor
          show_states: true
        graph_span: 12h
        span:
          end: minute
        yaxis:
          - id: temp
            decimals: 1
            apex_config:
              title: { text: "°C" }
          - id: hz
            opposite: true
            decimals: 0
            apex_config:
              title: { text: "Hz / rpm / L-min" }
        apex_config:
          chart:
            height: 360
        series:
          - entity: sensor.hph_source_outdoor_temp
            name: Outdoor
            yaxis_id: temp
            color: "#e0a800"
          - entity: sensor.hph_source_inlet_temp
            name: Return
            yaxis_id: temp
            color: "#7ec1ff"
          - entity: sensor.hph_source_outlet_temp
            name: Supply
            yaxis_id: temp
            color: "#d62728"
          - entity: sensor.hph_source_discharge_temp
            name: Discharge
            yaxis_id: temp
            color: "#2ca02c"
          - entity: sensor.hph_source_compressor_freq
            name: Compressor frequency
            yaxis_id: hz
            color: "#bcbd22"
          - entity: sensor.hph_source_flow_rate
            name: Water flow
            yaxis_id: hz
            color: "#7fb3d5"

      - type: custom:apexcharts-card
        header:
          show: true
          title: COP trend
        graph_span: 24h
        yaxis:
          - min: 0
            max: 6
            decimals: 1
        series:
          - entity: sensor.hph_cop_live
            name: COP (live)
            type: line
            color: "#17a2b8"
            stroke_width: 2
            extend_to: now
        apex_config:
          chart:
            height: 280
          markers:
            size: 3
          stroke:
            curve: smooth
          noData:
            text: "Sammele Daten — der Trend füllt sich, sobald der Kompressor läuft."

      # ── Heatmap: COP by hour-of-day (last 30 days) ──
      # Reveals when the system is least efficient (e.g. cold mornings,
      # DHW pulls in the evening). Uses top-level chart_type: heatmap
      # — apexcharts-card requires this matches series.type.
      - type: custom:apexcharts-card
        header:
          show: true
          title: COP heatmap — hour-of-day (last 30 days)
        chart_type: heatmap
        graph_span: 30d
        apex_config:
          chart:
            height: 320
          dataLabels:
            enabled: false
          plotOptions:
            heatmap:
              colorScale:
                ranges:
                  - { from: 0,   to: 1,   color: "#7f1d1d", name: "very poor (<1)" }
                  - { from: 1,   to: 2,   color: "#dc2626", name: "poor (1-2)" }
                  - { from: 2,   to: 3,   color: "#f59e0b", name: "marginal (2-3)" }
                  - { from: 3,   to: 4,   color: "#facc15", name: "ok (3-4)" }
                  - { from: 4,   to: 5,   color: "#84cc16", name: "good (4-5)" }
                  - { from: 5,   to: 100, color: "#15803d", name: "excellent (5+)" }
          noData:
            text: "Sammele Daten — Heatmap füllt sich nach mehreren Betriebstagen."
        series:
          - entity: sensor.hph_cop_live
            name: COP
            type: heatmap
            group_by:
              func: avg
              duration: 1h

      # ── Scatter: COP vs daily energy (correlation lens) ──
      # Plots daily COP over time; "extend_to: false" + numeric xaxis
      # caused an apexcharts-card config error. Simplified to a daily
      # COP timeline with markers.
      - type: custom:apexcharts-card
        header:
          show: true
          title: COP daily — last 30 days
        graph_span: 30d
        yaxis:
          - min: 0
            max: 6
            decimals: 1
        series:
          - entity: sensor.hph_cop_daily
            name: Daily COP
            type: line
            color: "#17a2b8"
            stroke_width: 0
            statistics:
              type: state
              period: day
        apex_config:
          chart:
            height: 280
          markers:
            size: 6
          noData:
            text: "Sammele Daten — tägliche COP-Punkte erscheinen nach jedem vollen Tag."

  # ════════════════════════════════════════════════════════════════════════
  # 4) EFFICIENCY — period KPIs, comparisons, tariff splits, multi-year trend
  # ════════════════════════════════════════════════════════════════════════
  - title: Efficiency
    path: efficiency
    icon: mdi:chart-bar
    cards:
      # ── Headline trend card ──
      - type: custom:mushroom-template-card
        primary: Efficiency trend
        secondary: "{{ state_attr('sensor.hph_efficiency_trend','headline') }}"
        icon: |-
          {% set s = states('sensor.hph_efficiency_trend') %}
          {% if s == 'great' %}mdi:rocket-launch
          {% elif s == 'better' %}mdi:trending-up
          {% elif s == 'critical' %}mdi:alert
          {% elif s == 'worse' %}mdi:trending-down
          {% else %}mdi:trending-neutral{% endif %}
        icon_color: |-
          {% set s = states('sensor.hph_efficiency_trend') %}
          {% if s == 'great' %}green
          {% elif s == 'better' %}light-green
          {% elif s == 'critical' %}red
          {% elif s == 'worse' %}orange
          {% else %}grey{% endif %}
        multiline_secondary: true
        fill_container: true

      # ── Side-by-side comparison cards (month / year) ──
      - type: horizontal-stack
        cards:
          - type: custom:mushroom-template-card
            primary: vs last month
            secondary: |-
              {% set p = states('sensor.hph_cop_change_month_pct') | float(0) %}
              {% set cur = states('sensor.hph_cop_monthly') %}
              {% set prev = states('sensor.hph_cop_last_month') %}
              {% if prev | float(0) <= 0.1 %}
                no data yet
              {% else %}
                COP {{ cur }} vs {{ prev }} ({{ '+' if p > 0 else '' }}{{ p }}%)
              {% endif %}
            icon: |-
              {% set p = states('sensor.hph_cop_change_month_pct') | float(0) %}
              {% if p > 1 %}mdi:arrow-up-bold
              {% elif p < -1 %}mdi:arrow-down-bold
              {% else %}mdi:equal{% endif %}
            icon_color: |-
              {% set p = states('sensor.hph_cop_change_month_pct') | float(0) %}
              {% if p > 1 %}green
              {% elif p < -1 %}red
              {% else %}grey{% endif %}
            multiline_secondary: true
            fill_container: true

          - type: custom:mushroom-template-card
            primary: vs last year
            secondary: |-
              {% set p = states('sensor.hph_scop_change_year_pct') | float(0) %}
              {% set cur = states('sensor.hph_scop') %}
              {% set prev = states('sensor.hph_cop_last_year') %}
              {% if prev | float(0) <= 0.1 %}
                no data yet
              {% else %}
                SCOP {{ cur }} vs {{ prev }} ({{ '+' if p > 0 else '' }}{{ p }}%)
              {% endif %}
            icon: |-
              {% set p = states('sensor.hph_scop_change_year_pct') | float(0) %}
              {% if p > 1 %}mdi:arrow-up-bold
              {% elif p < -1 %}mdi:arrow-down-bold
              {% else %}mdi:equal{% endif %}
            icon_color: |-
              {% set p = states('sensor.hph_scop_change_year_pct') | float(0) %}
              {% if p > 1 %}green
              {% elif p < -1 %}red
              {% else %}grey{% endif %}
            multiline_secondary: true
            fill_container: true

      # ── Energy comparison cards ──
      - type: horizontal-stack
        cards:
          - type: custom:mushroom-template-card
            primary: Thermal energy this month
            secondary: |-
              {% set cur = states('sensor.hph_thermal_monthly') | float(0) %}
              {% set prev = states('sensor.hph_thermal_last_month') | float(0) %}
              {% set p = states('sensor.hph_thermal_change_month_pct') | float(0) %}
              {{ cur | round(2) }} kWh{% if prev > 0.1 %} ({{ '+' if p > 0 else '' }}{{ p }}% vs {{ prev | round(2) }} kWh){% endif %}
            icon: mdi:fire
            icon_color: red
            multiline_secondary: true
            fill_container: true

          - type: custom:mushroom-template-card
            primary: Electrical energy this month
            secondary: |-
              {% set cur = states('sensor.hph_electrical_monthly') | float(0) %}
              {% set prev = states('sensor.hph_electrical_last_month') | float(0) %}
              {% set p = states('sensor.hph_electrical_change_month_pct') | float(0) %}
              {{ cur | round(2) }} kWh{% if prev > 0.1 %} ({{ '+' if p > 0 else '' }}{{ p }}% vs {{ prev | round(2) }} kWh){% endif %}
            icon: mdi:flash
            icon_color: yellow
            multiline_secondary: true
            fill_container: true

      # ── Comparison detail (entities) ──
      - type: entities
        title: Comparisons — detail
        entities:
          - entity: sensor.hph_cop_monthly
            name: COP this month
          - entity: sensor.hph_cop_last_month
            name: COP last month
          - entity: sensor.hph_cop_change_month_pct
            name: Change vs last month
          - type: divider
          - entity: sensor.hph_scop
            name: SCOP year to date
          - entity: sensor.hph_cop_last_year
            name: SCOP previous year
          - entity: sensor.hph_scop_change_year_pct
            name: Change vs last year
          - type: divider
          - entity: sensor.hph_thermal_last_month
            name: Thermal — last month
          - entity: sensor.hph_electrical_last_month
            name: Electrical — last month
          - entity: sensor.hph_thermal_last_year
            name: Thermal — last year
          - entity: sensor.hph_electrical_last_year
            name: Electrical — last year

      # ── Overall KPIs ──
      - type: entities
        title: Overall COP / SCOP
        entities:
          - entity: sensor.hph_scop
            name: SCOP (year to date)
          - entity: sensor.hph_cop_monthly
            name: COP this month
          - entity: sensor.hph_cop_daily
            name: COP today
          - entity: sensor.hph_cop_live
            name: COP live

      # ── Mode-split COP ──
      - type: entities
        title: COP by mode
        entities:
          - entity: sensor.hph_cop_yearly_heating
            name: SCOP heating (year)
          - entity: sensor.hph_cop_monthly_heating
            name: COP heating (month)
          - entity: sensor.hph_cop_yearly_dhw
            name: SCOP DHW (year)
          - entity: sensor.hph_cop_monthly_dhw
            name: COP DHW (month)

      # ── Energy totals (markdown for 2-decimal display — utility_meter
      # accumulates with float precision, has no round option, and the
      # entity-registry display_precision can't be set from YAML) ──
      - type: markdown
        title: Energy totals
        content: |
          | | Thermal | Electrical |
          |---|---:|---:|
          | **Year** | {{ states('sensor.hph_thermal_yearly') \| float(0) \| round(2) }} kWh | {{ states('sensor.hph_electrical_yearly') \| float(0) \| round(2) }} kWh |
          | **Month** | {{ states('sensor.hph_thermal_monthly') \| float(0) \| round(2) }} kWh | {{ states('sensor.hph_electrical_monthly') \| float(0) \| round(2) }} kWh |
          | **Today** | {{ states('sensor.hph_thermal_daily') \| float(0) \| round(2) }} kWh | {{ states('sensor.hph_electrical_daily') \| float(0) \| round(2) }} kWh |

      # ── Cost overview ──
      - type: entities
        title: Electricity costs
        entities:
          - entity: sensor.hph_cost_today
            name: Cost today (€)
          - entity: sensor.hph_cost_monthly
            name: Cost this month (€)
          - entity: sensor.hph_cost_seasonal
            name: Cost this season (€)
          - type: divider
          - entity: sensor.hph_electricity_price_effective
            name: Effective price (ct/kWh)
          - entity: number.hph_electricity_price_ct_per_kwh
            name: Manual price fallback (ct/kWh)
          - entity: text.hph_electricity_price_entity
            name: Live price sensor (entity-ID, optional)

      # ── Energy split by mode (this month) ──
      - type: markdown
        title: Energy by mode — this month
        content: |
          | Mode | Thermal | Electrical |
          |---|---:|---:|
          | Heating | {{ states('sensor.hph_thermal_monthly_split_heating') \| float(0) \| round(2) }} kWh | {{ states('sensor.hph_electrical_monthly_split_heating') \| float(0) \| round(2) }} kWh |
          | DHW | {{ states('sensor.hph_thermal_monthly_split_dhw') \| float(0) \| round(2) }} kWh | {{ states('sensor.hph_electrical_monthly_split_dhw') \| float(0) \| round(2) }} kWh |
          | Cooling | {{ states('sensor.hph_thermal_monthly_split_cooling') \| float(0) \| round(2) }} kWh | {{ states('sensor.hph_electrical_monthly_split_cooling') \| float(0) \| round(2) }} kWh |

      # ── 30-day bar chart ──
      - type: custom:apexcharts-card
        header:
          show: true
          title: Daily energy — last 30 days
          show_states: true
        graph_span: 30d
        yaxis:
          - id: energy
            min: 0
            decimals: 1
        series:
          - entity: sensor.hph_thermal_daily
            name: Thermal
            type: column
            color: "#d62728"
            yaxis_id: energy
            statistics:
              type: change
              period: day
              align: start
          - entity: sensor.hph_electrical_daily
            name: Electrical
            type: column
            color: "#bcbd22"
            yaxis_id: energy
            statistics:
              type: change
              period: day
              align: start
        apex_config:
          chart:
            height: 300
          yaxis:
            title:
              text: "kWh"
          noData:
            text: "Sammele Daten — Tageswerte erscheinen nach dem ersten vollen Tag."

      # ── COP trend (30 days) ──
      - type: custom:apexcharts-card
        header:
          show: true
          title: COP trend — last 30 days
        graph_span: 30d
        yaxis:
          - min: 0
            max: 6
            decimals: 1
        series:
          - entity: sensor.hph_cop_daily
            name: COP daily
            type: line
            color: "#17a2b8"
            stroke_width: 2
            statistics:
              type: state
              period: day
        apex_config:
          chart:
            height: 280
          markers:
            size: 4
          noData:
            text: "Sammele Daten — der 30-Tage-Trend füllt sich nach mehreren Tagen Betrieb."

      # ── SCOP per heating season (Jul 1 – Jun 30, last 10 seasons) ──
      # apexcharts-card span.end accepts day/hour/minute only — "year"
      # caused a configuration error. Use day end + a long graph_span.
      - type: custom:apexcharts-card
        header:
          show: true
          title: SCOP per heating season (Jul – Jun)
        graph_span: 3650d
        span:
          end: day
        series:
          - entity: sensor.hph_scop
            name: SCOP
            type: column
            color: "#66bb6a"
            statistics:
              type: mean
              period: month
              align: start
        apex_config:
          chart:
            height: 280
          yaxis:
            min: 0
            max: 7
            decimalsInFloat: 2
            title:
              text: "SCOP"
          xaxis:
            labels:
              format: "yyyy"
          noData:
            text: "Sammele Daten — SCOP-Trend füllt sich nach der ersten kompletten Heizsaison."

      # ── Statistics graph (built-in) ──
      - type: statistics-graph
        title: Cumulative energy (30 days)
        days_to_show: 30
        stat_types:
          - sum
        entities:
          - sensor.hph_thermal_energy_active
          - sensor.hph_electrical_energy_active

      - type: markdown
        content: |
          **HA Energy Dashboard:** add `sensor.hph_thermal_energy_active`
          and `sensor.hph_electrical_energy_active` as *Individual
          Devices* under Settings → Energy. These follow your selected source
          (calculated / external power / external energy meter), so the
          Energy Dashboard always shows the correct counter.

          **Multi-year comparison:** import the Grafana dashboard
          `efficiency_jaz_maz.json` for cross-year SCOP/MAZ charts
          (see [docs/installation.md](/local/hph/installation.md)).

  # ════════════════════════════════════════════════════════════════════════
  # 5) OPTIMIZATION — cycle analysis + advisor + control switches
  # ════════════════════════════════════════════════════════════════════════
  - title: Optimization
    path: optimization
    icon: mdi:tune-vertical
    cards:
      - type: custom:mushroom-template-card
        primary: Aggregate verdict
        secondary: >-
          {% set s = states('sensor.hph_advisor_summary') %}
          {% if s == 'critical' %}🔴 Action required
          {% elif s == 'warn' %}🟡 Warnings present
          {% else %}🟢 All clear{% endif %}
        icon: mdi:lightbulb-on
        icon_color: |-
          {% set s = states('sensor.hph_advisor_summary') %}
          {% if s == 'critical' %}red{% elif s == 'warn' %}yellow{% else %}green{% endif %}

      - type: entities
        title: Cycle analysis (today)
        entities:
          - sensor.hph_cycles_per_hour
          - number.hph_cycles_today
          - number.hph_short_cycles_today
          - sensor.hph_short_cycle_ratio
          - sensor.hph_avg_cycle_duration_24h
          - number.hph_cycle_last_duration_min
          - number.hph_cycle_last_pause_min
          - number.hph_cycle_short_threshold_min

      - type: custom:apexcharts-card
        header:
          show: true
          title: Cycling — last 7 days
        graph_span: 7d
        span:
          end: day
        series:
          - entity: number.hph_cycles_today
            name: Cycles/day
            type: column
            group_by:
              func: max
              duration: 1d
              start_with_last: false
          - entity: number.hph_short_cycles_today
            name: Short cycles/day
            type: column
            color: "#d62728"
            group_by:
              func: max
              duration: 1d
              start_with_last: false
        apex_config:
          chart:
            height: 260
          noData:
            text: "Sammele Daten — Tageswerte erscheinen, sobald der Kompressor läuft."

      - type: entities
        title: Advisor — recommendations
        entities:
          - entity: sensor.hph_advisor_short_cycle
            name: Cycling
            secondary_info: last-changed
          - entity: sensor.hph_advisor_spread
            name: Supply/return spread
          - entity: sensor.hph_advisor_defrost
            name: Defrost behaviour
          - entity: sensor.hph_advisor_heat_curve
            name: Heating curve / aux heater
          - entity: sensor.hph_advisor_dhw_runtime
            name: DHW run length
          - entity: sensor.hph_advisor_heating_limit
            name: Heating limit (auto-detected)
          - entity: sensor.hph_advisor_pressure_trend
            name: Water pressure trend (leak detection)
          - entity: sensor.hph_advisor_diagnostics
            name: Diagnostics (active fault / recurrence)
          - entity: sensor.hph_advisor_analysis
            name: Analysis (indoor temp deviation, curve recommendation)
          - entity: sensor.hph_advisor_pump_curve
            name: Pump curve (7-day spread mean / stdev)
          - entity: sensor.hph_advisor_efficiency_drift
            name: Efficiency drift (year-over-year SCOP)
          - entity: sensor.hph_advisor_dhw_timing
            name: DHW timing (fires per day)
        state_color: true

      # ── Analysis recommendation card ──
      - type: custom:mushroom-template-card
        primary: |-
          {% set rec = state_attr('sensor.hph_advisor_analysis','recommendation_k') | float(0) %}
          {% if rec == 0 %}Heating curve in band
          {% elif rec > 0 %}Recommend heating curve +{{ rec }} K
          {% else %}Recommend heating curve {{ rec }} K{% endif %}
        secondary: >-
          {{ state_attr('sensor.hph_advisor_analysis','message') }}
        icon: |-
          {% set s = states('sensor.hph_advisor_analysis') %}
          {% if s == 'critical' %}mdi:alert-octagon
          {% elif s == 'warn' %}mdi:alert
          {% elif s == 'info' %}mdi:lightbulb-on-outline
          {% else %}mdi:check-circle{% endif %}
        icon_color: |-
          {% set s = states('sensor.hph_advisor_analysis') %}
          {% if s == 'critical' %}red
          {% elif s == 'warn' %}orange
          {% elif s == 'info' %}blue
          {% else %}green{% endif %}
        multiline_secondary: true
        fill_container: true

      # ── Active fault / diagnostics card ──
      - type: custom:mushroom-template-card
        primary: |-
          {% set s = states('sensor.hph_diagnostics_current_error') %}
          {% if s == 'ok' %}No active fault
          {% else %}Fault: {{ s }}{% endif %}
        secondary: >-
          {{ state_attr('sensor.hph_diagnostics_current_error','message') }}
        icon: |-
          {% set sev = state_attr('sensor.hph_diagnostics_current_error','severity') %}
          {% if sev == 'critical' %}mdi:alert-octagon
          {% elif sev == 'warn' %}mdi:alert
          {% elif sev == 'info' %}mdi:information-outline
          {% else %}mdi:check-circle{% endif %}
        icon_color: |-
          {% set sev = state_attr('sensor.hph_diagnostics_current_error','severity') %}
          {% if sev == 'critical' %}red
          {% elif sev == 'warn' %}orange
          {% elif sev == 'info' %}blue
          {% else %}green{% endif %}
        multiline_secondary: true
        fill_container: true

      - type: entities
        title: Diagnostics — detail
        entities:
          - entity: sensor.hph_diagnostics_current_error
            name: Active fault code
          - entity: sensor.hph_diagnostics_recurrence
            name: Recurrences (last 5 events)
          - entity: datetime.hph_diag_last_error_time
            name: Last error timestamp
          - entity: text.hph_diag_error_history
            name: Recent error history (JSON)

      # Heating limit — observed.
      # The smoothed sensor is a platform:statistics sensor that needs the
      # source (number.hph_heating_limit_observed_c) to receive at least one
      # sample within the 7-day window before it becomes available. Show a
      # friendly fallback if the sensor isn't ready yet.
      - type: conditional
        conditions:
          - entity: sensor.hph_heating_limit_smoothed
            state_not: unavailable
          - entity: sensor.hph_heating_limit_smoothed
            state_not: unknown
        card:
          type: entities
          title: Heating limit — observed
          entities:
            - entity: sensor.hph_heating_limit_smoothed
              name: Smoothed (7-day rolling avg)
            - entity: number.hph_heating_limit_observed_c
              name: Last observation
            - entity: number.hph_advisor_heating_limit_target_c
              name: Target setpoint
      - type: conditional
        conditions:
          - entity: sensor.hph_heating_limit_smoothed
            state:
              - unavailable
              - unknown
        card:
          type: markdown
          title: Heating limit — observed
          content: |
            Wird verfügbar, sobald der erste **Kompressorlauf ≥ 30 min**
            mit anschließendem Stopp aufgezeichnet wurde. Der Wert ist ein
            7-Tage-rollierender Durchschnitt der Außentemperatur beim
            Stoppen — er zeigt, ab welcher Außentemperatur deine
            Heizung aufhört zu laufen.

            **Zielwert:** {{ states('number.hph_advisor_heating_limit_target_c') }} °C

      - type: markdown
        title: Recommendation detail
        content: |
          ### Cycling
          {{ state_attr('sensor.hph_advisor_short_cycle','message') }}

          ### Spread
          {{ state_attr('sensor.hph_advisor_spread','message') }}

          ### Defrost
          {{ state_attr('sensor.hph_advisor_defrost','message') }}

          ### Heating curve / aux heater
          {{ state_attr('sensor.hph_advisor_heat_curve','message') }}

          ### DHW
          {{ state_attr('sensor.hph_advisor_dhw_runtime','message') }}

          ### Heating limit
          {{ state_attr('sensor.hph_advisor_heating_limit','message') }}

          ### Diagnostics
          {{ state_attr('sensor.hph_advisor_diagnostics','message') }}

          ### Water pressure trend
          {{ state_attr('sensor.hph_advisor_pressure_trend','message') }}

          ### Pump curve (v0.8)
          {{ state_attr('sensor.hph_advisor_pump_curve','message') }}

          ### Efficiency drift (v0.8)
          {{ state_attr('sensor.hph_advisor_efficiency_drift','message') }}

          ### DHW timing (v0.8)
          {{ state_attr('sensor.hph_advisor_dhw_timing','message') }}

      - type: entities
        title: Control strategies (HeishaMoNR parity)
        entities:
          - switch.hph_ctrl_master
          - switch.hph_ctrl_ccc
          - number.hph_ctrl_ccc_min_pause_min
          - number.hph_ctrl_ccc_min_runtime_min
          - switch.hph_ctrl_quiet_night
          - switch.hph_ctrl_solar_dhw
          - text.hph_ctrl_pv_surplus_entity
          - number.hph_ctrl_solar_pv_threshold_w

      - type: entities
        title: Control extensions (v0.7) — adaptive curve / price / forecast
        entities:
          - type: section
            label: Adaptive heating curve (self-learning, weekly)
          - switch.hph_ctrl_adaptive_curve
          - number.hph_ctrl_adaptive_max_step_k
          - number.hph_ctrl_adaptive_supply_min_c
          - number.hph_ctrl_adaptive_supply_max_c
          - datetime.hph_ctrl_adaptive_last_run
          - type: section
            label: Price-driven DHW (Tibber / aWATTar)
          - switch.hph_ctrl_price_dhw
          - text.hph_ctrl_price_entity
          - text.hph_ctrl_price_mean_entity
          - number.hph_ctrl_price_threshold_factor
          - number.hph_ctrl_price_max_per_day
          - number.hph_ctrl_price_dhw_fires_today
          - datetime.hph_ctrl_price_dhw_last_fire
          - type: section
            label: Weather-forecast pre-heating
          - switch.hph_ctrl_forecast_preheat
          - text.hph_ctrl_forecast_entity
          - number.hph_ctrl_forecast_drop_threshold_k
          - number.hph_ctrl_forecast_boost_k
          - switch.hph_ctrl_forecast_preheat_active

      - type: entities
        title: Advisor thresholds (power-user tweaks)
        entities:
          - number.hph_advisor_short_cycle_warn_pct
          - number.hph_advisor_short_cycle_crit_pct
          - number.hph_advisor_dt_target_k
          - number.hph_advisor_dhw_min_runtime_min
          - type: section
            label: v0.8 advisors
          - number.hph_advisor_drift_warn_pct
          - number.hph_advisor_drift_crit_pct
          - number.hph_advisor_dhw_max_fires_per_day
          - number.hph_advisor_pump_spread_min_samples

  # ════════════════════════════════════════════════════════════════════════
  # 5b) PROGRAMS — multi-day / scheduled service programs (v0.8)
  # ════════════════════════════════════════════════════════════════════════
  - title: Programs
    path: programs
    icon: mdi:calendar-clock
    cards:
      - type: markdown
        content: |
          ## Service programs
          Multi-day and scheduled service programs. All gated by the
          master switch `Control master` — you must enable both the master
          and the per-program switch for anything to actually run. The
          status cards below show the current state at a glance.

          Programs write directly to the heat pump via the configured
          control write-helpers (see Configuration → Control write
          targets). If a program does nothing when triggered, double-check
          those helpers point at existing entities.

      # ── Status cards ──
      - type: custom:mushroom-template-card
        primary: Screed dry-out
        secondary: |-
          {% set s = states('sensor.hph_prog_screed_status') %}
          {% set d = states('number.hph_prog_screed_day') | int(0) %}
          {% set t = states('sensor.hph_prog_screed_target') %}
          {% if s == 'disabled' %}Disabled
          {% elif s == 'armed' %}Armed — not yet running
          {% elif s == 'running' %}Day {{ d }} — target {{ t }} °C
          {% else %}{{ s }}{% endif %}
        icon: mdi:home-floor-a
        icon_color: |-
          {% set s = states('sensor.hph_prog_screed_status') %}
          {% if s == 'running' %}orange
          {% elif s == 'armed' %}blue
          {% else %}grey{% endif %}
        multiline_secondary: true
        fill_container: true

      # ── Master-switch reminder ──
      - type: entities
        title: Master switch (required for all control + programs)
        entities:
          - entity: switch.hph_ctrl_master
            name: Control master switch

      # ── Legionella ──
      - type: markdown
        content: |
          ### Legionella protection
          Configure the weekly anti-legionella DHW boost directly on
          your heat pump controller or via the vendor app. Trigger a
          one-off cycle using the **Run legionella program now** button
          from **Settings → Devices & Services → HeatPump Hero**.

      # ── Screed dry-out controls ──
      - type: entities
        title: Screed dry-out program
        entities:
          - type: section
            label: Profile selection (pick BEFORE arming)
          - entity: select.hph_prog_screed_profile
            name: Profile
          - type: section
            label: Arm / disarm
          - entity: switch.hph_prog_screed
            name: Enable program (arming triggers Day 1)
          - entity: switch.hph_prog_screed_active
            name: Currently running (status flag)
          - type: section
            label: Status
          - entity: datetime.hph_prog_screed_start
            name: Program start
          - entity: number.hph_prog_screed_day
            name: Current day index
          - entity: sensor.hph_prog_screed_target
            name: Today's supply target (°C)
          - entity: sensor.hph_prog_screed_status
            name: Status

      # ── Profile reference ──
      - type: markdown
        title: Profile reference
        content: |
          | Day | functional_3d | combined_10d | din_18560_28d |
          |---:|---:|---:|---:|
          | 1 | 25 °C | 25 °C | 25 °C |
          | 2 | 45 °C | 45 °C | 25 °C |
          | 3 | 25 °C | 25 °C | 25 °C |
          | 4 | end | 40 °C | 30 °C |
          | 5 |  | 40 °C | 35 °C |
          | 6 |  | 40 °C | 45 °C |
          | 7-24 |  | 40 / 35 / 30 / 25 then end | 50 °C |
          | 25-28 |  |  | 45 / 35 / 30 / 25 |

          **functional_3d** — ISO EN 1264-4 §4.4.2 functional heating,
          minimum proof of operation.

          **combined_10d** — common compromise: 3 d functional + 7 d
          curing taper.

          **din_18560_28d** — full 28-day cement-screed protocol per
          DIN 18560-1.

          The current day's target is pushed to the configured Z1
          heat-curve target_high entity at 00:01 daily. Curve is NOT
          restored on program end — note your pre-program values from
          Configuration → Heating curve.

  # ════════════════════════════════════════════════════════════════════════
  # 6) MOBILE — slim, single-column layout for phones
  # ════════════════════════════════════════════════════════════════════════
  - title: Mobile
    path: mobile
    icon: mdi:cellphone
    type: panel
    badges: []
    cards:
      - type: vertical-stack
        cards:
          # ── Hero card ──
          - type: custom:mushroom-template-card
            primary: HeatPump Hero
            secondary: |-
              {% set s = states('sensor.hph_efficiency_trend') %}
              {% if s == 'great' %}🚀 great
              {% elif s == 'better' %}📈 trending up
              {% elif s == 'critical' %}⚠️ check advisor
              {% elif s == 'worse' %}📉 trending down
              {% else %}— stable —{% endif %}
              · COP live {{ states('sensor.hph_cop_live') }}
            icon: mdi:heat-pump
            icon_color: |-
              {% set s = states('sensor.hph_efficiency_trend') %}
              {% if s == 'great' %}green
              {% elif s == 'better' %}light-green
              {% elif s == 'critical' %}red
              {% elif s == 'worse' %}orange
              {% else %}grey{% endif %}
            multiline_secondary: true
            fill_container: true

          # ── Compact KPI strip ──
          - type: custom:mushroom-chips-card
            chips:
              - type: template
                icon: mdi:thermometer
                icon_color: blue
                content: "{{ states('sensor.hph_source_outdoor_temp') }}°"
              - type: template
                icon: mdi:water-thermometer
                icon_color: red
                content: "{{ states('sensor.hph_source_outlet_temp') }}°"
              - type: template
                icon: mdi:water-pump
                icon_color: blue
                content: "{{ states('sensor.hph_source_flow_rate') }}"
              - type: template
                icon: mdi:sine-wave
                icon_color: |-
                  {{ 'green' if is_state('binary_sensor.hph_compressor_running','on') else 'disabled' }}
                content: "{{ states('sensor.hph_source_compressor_freq') }}Hz"
              - type: template
                icon: mdi:gauge
                icon_color: |-
                  {% set v = states('sensor.hph_cop_live') | float(0) %}
                  {% if v >= 4 %}green{% elif v >= 3 %}yellow{% elif v > 0 %}orange{% else %}disabled{% endif %}
                content: "{{ states('sensor.hph_cop_live') }}"
              - type: template
                icon: mdi:snowflake-melt
                icon_color: |-
                  {{ 'cyan' if is_state('binary_sensor.hph_defrost_active','on') else 'disabled' }}
                content: ""

          # ── Compact period-COP grid ──
          - type: grid
            columns: 3
            square: false
            cards:
              - type: custom:mushroom-template-card
                primary: Today
                secondary: "COP {{ states('sensor.hph_cop_daily') }}"
                icon: mdi:calendar-today
                fill_container: true
              - type: custom:mushroom-template-card
                primary: Month
                secondary: "COP {{ states('sensor.hph_cop_monthly') }}"
                icon: mdi:calendar-month
                fill_container: true
              - type: custom:mushroom-template-card
                primary: Year
                secondary: "SCOP {{ states('sensor.hph_scop') }}"
                icon: mdi:calendar
                fill_container: true

          # ── Live trend ──
          - type: custom:apexcharts-card
            header:
              show: true
              title: Last 6h
            graph_span: 6h
            apex_config:
              chart: { height: 200 }
              legend: { show: false }
            yaxis:
              - id: temp
                decimals: 1
              - id: hz
                opposite: true
                decimals: 0
            series:
              - entity: sensor.hph_source_outlet_temp
                name: Supply
                yaxis_id: temp
                color: "#d62728"
                stroke_width: 2
              - entity: sensor.hph_source_inlet_temp
                name: Return
                yaxis_id: temp
                color: "#7ec1ff"
                stroke_width: 2
              - entity: sensor.hph_source_compressor_freq
                name: Hz
                yaxis_id: hz
                color: "#bcbd22"
                stroke_width: 1.5

          # ── Active fault (only shown when present) ──
          - type: conditional
            conditions:
              - entity: sensor.hph_diagnostics_current_error
                state_not: ok
            card:
              type: custom:mushroom-template-card
              primary: |-
                Fault: {{ states('sensor.hph_diagnostics_current_error') }}
              secondary: >-
                {{ state_attr('sensor.hph_diagnostics_current_error','message') }}
              icon: mdi:alert-octagon
              icon_color: red
              multiline_secondary: true
              fill_container: true
              tap_action:
                action: navigate
                navigation_path: /hph/optimization

          # ── Advisor verdict ──
          - type: custom:mushroom-template-card
            primary: Advisor
            secondary: >-
              {% set s = states('sensor.hph_advisor_summary') %}
              {% if s == 'critical' %}🔴 action required
              {% elif s == 'warn' %}🟡 {{ state_attr('sensor.hph_advisor_summary','warn_count') }} warning(s)
              {% else %}🟢 all clear{% endif %}
            icon: mdi:lightbulb-on
            icon_color: |-
              {% set s = states('sensor.hph_advisor_summary') %}
              {% if s == 'critical' %}red{% elif s == 'warn' %}yellow{% else %}green{% endif %}
            tap_action:
              action: navigate
              navigation_path: /hph/optimization

          # ── Programs status ──
          - type: conditional
            conditions:
              - entity: sensor.hph_prog_screed_status
                state: running
            card:
              type: custom:mushroom-template-card
              primary: Screed running
              secondary: >-
                Day {{ states('number.hph_prog_screed_day') }} —
                target {{ states('sensor.hph_prog_screed_target') }} °C
              icon: mdi:home-floor-a
              icon_color: orange
              tap_action:
                action: navigate
                navigation_path: /hph/programs
            fill_container: true

  # ════════════════════════════════════════════════════════════════════════
  # 7) CONFIGURATION
  # ════════════════════════════════════════════════════════════════════════
  - title: Configuration
    path: config
    icon: mdi:cog
    cards:
      - type: markdown
        content: |
          ## Source adapter
          HeatPump Hero reads everything via the configurable source helpers
          below. Defaults match the kamaradclimber Heishamon integration —
          if you use a different heat pump or different meters, change the
          relevant entity-ID and HeatPump Hero follows automatically. No YAML
          editing required.

      # ── Vendor preset & model selector (1-click setup) ──
      - type: entities
        title: Vendor & model
        entities:
          - entity: select.hph_vendor_preset
            name: Vendor preset (auto-fills source helpers)
            secondary_info: last-changed
          - entity: select.hph_pump_model
            name: Heat-pump model
          - entity: sensor.hph_model_description
            name: Model description
          - entity: sensor.hph_model_refrigerant
            name: Refrigerant

      - type: entities
        title: Model thresholds (auto-set on model change, override OK)
        entities:
          - entity: number.hph_model_compressor_min_hz
            name: Compressor min Hz
          - entity: number.hph_model_compressor_max_hz
            name: Compressor max Hz
          - entity: number.hph_model_min_flow_lpm
            name: Minimum water flow (L/min)
          - entity: number.hph_model_max_supply_c
            name: Maximum supply temperature (°C)

      # ── Source-mode selectors ──
      - type: entities
        title: Source modes
        entities:
          - entity: select.hph_thermal_source
            name: Thermal source mode
          - entity: select.hph_electrical_source
            name: Electrical source mode
          - entity: select.hph_schema_variant
            name: Schematic variant (auto / manual)
          - entity: sensor.hph_schema_variant_active
            name: Schematic — active
          - entity: sensor.hph_schema_variant_detected
            name: Schematic — auto-detected

      # ── Optional-component sources (zone 2 / DHW / buffer) ──
      # Set the entity-ID to point at the right sensor, or leave blank to
      # mark the component as definitely absent. Detection follows.
      # ── Analysis sources (KI-Modul Layer 1) ──
      - type: entities
        title: Analysis (recommendations from observation)
        entities:
          - entity: text.hph_indoor_temp_entity
            name: Indoor reference sensor (entity-ID)
          - entity: text.hph_indoor_target_entity
            name: Indoor target sensor (entity-ID, optional)
          - entity: number.hph_indoor_target_default
            name: Fallback indoor target (°C)
          - entity: number.hph_analysis_window_days
            name: Observation window (days)
          - entity: number.hph_analysis_dead_band_k
            name: Dead band (K)
          - entity: sensor.hph_indoor_deviation_smoothed
            name: Smoothed deviation
          - entity: text.hph_heating_curve_recommendation
            name: Heating-curve script output

      - type: entities
        title: Optional components
        entities:
          - entity: text.hph_src_zone1_temp
            name: Zone 1 temperature
          - entity: text.hph_src_zone2_temp
            name: Zone 2 temperature (blank if absent)
          - entity: text.hph_src_dhw_temp
            name: DHW tank temperature (blank if absent)
          - entity: text.hph_src_dhw_target_temp
            name: DHW target temperature
          - entity: text.hph_src_buffer_temp
            name: Buffer tank temperature (blank if absent)
          - entity: text.hph_src_error_code
            name: Error / fault code source
          - type: divider
          - entity: binary_sensor.hph_has_hk2
            name: HK2 detected
          - entity: binary_sensor.hph_has_dhw
            name: DHW detected
          - entity: binary_sensor.hph_has_buffer
            name: Buffer detected

      # ── Heat pump device entities (UI-configurable) ──
      - type: entities
        title: Heat pump entities (configurable)
        entities:
          - entity: text.hph_src_inlet_temp
            name: Inlet (return) T°
          - entity: text.hph_src_outlet_temp
            name: Outlet (supply) T°
          - entity: text.hph_src_flow_rate
            name: Water flow rate
          - entity: text.hph_src_outdoor_temp
            name: Outdoor T°
          - entity: text.hph_src_compressor_freq
            name: Compressor frequency
          - entity: text.hph_src_compressor_hours
            name: Compressor hours (lifetime)
          - entity: text.hph_src_compressor_starts
            name: Compressor starts (lifetime)
          - entity: text.hph_src_discharge_temp
            name: Discharge T°
          - entity: text.hph_src_pump_pressure
            name: Water pressure
          - entity: text.hph_src_defrost_state
            name: Defrost state (binary)
          - entity: text.hph_src_aux_heater_state
            name: Aux heater state (binary)
          - entity: text.hph_src_operating_mode
            name: Operating mode (select)
          - entity: text.hph_src_internal_power
            name: Internal power consumption (W)

      # ── Control write targets (v0.8 vendor adapter) ──
      # Auto-filled by the vendor preset above; override here only if
      # your install uses non-standard entity-IDs (e.g. renamed entities,
      # second heat-pump unit). All control automations and programs
      # write through these helpers.
      - type: entities
        title: Control write targets (v0.8 vendor adapter)
        entities:
          - entity: text.hph_ctrl_write_quiet_mode
            name: Quiet-mode select
          - entity: text.hph_ctrl_write_force_dhw
            name: Force-DHW button
          - entity: text.hph_ctrl_write_z1_curve_high
            name: Z1 heat-curve target_high (number)
          - entity: text.hph_ctrl_write_z1_curve_low
            name: Z1 heat-curve target_low (number)
          - entity: text.hph_ctrl_write_dhw_target
            name: DHW target temperature (number)

      # ── External meter entities (optional) ──
      - type: entities
        title: External meters (optional)
        entities:
          - entity: text.hph_src_external_thermal_power
            name: External thermal power (W)
          - entity: text.hph_src_external_thermal_energy
            name: External heat meter (kWh, total_increasing)
          - entity: text.hph_src_external_electrical_power
            name: External electrical power (W)
          - entity: text.hph_src_external_electrical_energy
            name: External electricity meter (kWh, total_increasing)

      # ── Electricity cost settings ──
      - type: entities
        title: Electricity cost
        entities:
          - entity: number.hph_electricity_price_ct_per_kwh
            name: Manual price (ct/kWh)
          - entity: text.hph_electricity_price_entity
            name: Live price sensor (entity-ID, optional)
          - entity: sensor.hph_electricity_price_effective
            name: Effective price used (ct/kWh)
      - type: markdown
        content: |
          Set **Manual price** to your flat rate (e.g. 30 ct/kWh).
          Optionally enter the entity-ID of a live price sensor
          (e.g. `sensor.tibber_current_price` from the Tibber
          integration — unit must be **€/kWh**). When a live sensor
          is set and valid, it takes precedence over the manual price.
          Note: daily/monthly costs computed from a spot price are
          estimates; only a flat rate gives exact figures.

      # ── Export / scheduled task ──
      - type: entities
        title: Export (long-term values for external analysis)
        entities:
          - entity: select.hph_export_format
            name: Format
          - entity: select.hph_export_period
            name: Period
          - entity: text.hph_export_target_path
            name: Target directory
          - entity: select.hph_export_schedule
            name: Schedule
          - entity: datetime.hph_export_last_run
            name: Last run
          - entity: button.hph_export_now
            name: ▶ Export now (manual)

      # ── Multi-platform bridge (read-only) ──
      - type: entities
        title: Multi-platform bridge (read-only)
        entities:
          - entity: switch.hph_bridge_enabled
            name: Bridge enabled
          - entity: text.hph_bridge_prefix
            name: MQTT topic prefix
          - entity: sensor.hph_bridge_last_publish
            name: Last publish
      - type: markdown
        content: |
          When enabled, HeatPump Hero republishes ~50 derived sensors
          (COP / SCOP / advisor / diagnostics) onto MQTT under
          `<prefix>/<domain>/<entity_id>/state` and `/attributes`, so
          ioBroker / openHAB / Node-RED can read them. Read-only —
          control stays in HA. See
          [docs/multivendor_bridge.md](https://github.com/st03psn/heat-pump-hero/blob/main/docs/multivendor_bridge.md)
          for the topic schema and per-platform recipes.

      # ── Heat-curve auto-entities (Heishamon-specific UI shortcut) ──
      # Note: this filter pattern is heat-pump specific — adapt if you swap
      # to a non-Heishamon heat pump.
      - type: custom:auto-entities
        card:
          type: entities
          title: Heating curve — zone 1 (Heishamon-specific)
        filter:
          include:
            - entity_id: number.panasonic_heat_pump_main_z1_heat_curve_*
        show_empty: false
      - type: custom:auto-entities
        card:
          type: entities
          title: Heating curve — zone 2 (if present, Heishamon-specific)
        filter:
          include:
            - entity_id: number.panasonic_heat_pump_main_z2_heat_curve_*
        show_empty: false
